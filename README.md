# StickerMatch (iOS MVP)

A fan utility for completing a physical football sticker/card album. Track which
stickers you **have**, are **missing**, or have **repeated**, publish your spares +
needs to a marketplace, find **nearby** people whose album complements yours, and
arrange trades through **in-app chat** — no phone numbers shared.

> **Not an official app.** No FIFA / Panini / World Cup logos, player images, sticker
> artwork, or official album numbering/compilation are used. Player and country names are
> plain text only.
>
> **Safety:** _Meet only in public places. StickerMatch does not process payments, verify
> users, or guarantee trades._

No payments. Connections happen through in-app messaging; users can **block** and **report**.

> **Visibility model:** trade posts are public to **all signed-in users** (minus anyone you've
> blocked) and only while **active** — enforced server-side by RLS (`supabase/09_hardening2.sql`).
> "Nearby" is a **client-side** distance sort for convenience, _not_ a privacy boundary — so a
> post's meeting point/notes should never contain anything private.

---

## Tech stack

| Layer    | Choice                                                            |
|----------|-------------------------------------------------------------------|
| iOS app  | Swift, SwiftUI, MVVM, async/await                                 |
| Backend  | Supabase — Postgres + Auth + RLS + Realtime + Edge Functions      |
| Auth     | Google OAuth (working); Sign in with Apple (scaffolded — see checklist) |
| Location | CoreLocation (when-in-use) + MapKit city autocomplete            |
| Flags    | Loaded on demand from flagcdn.com (public-domain, no bundled assets) |

**Requirements:** Xcode 15+, iOS 17+ deployment target, [XcodeGen](https://github.com/yonaskolb/XcodeGen).

---

## Repository layout

```
supabase/
  01_schema.sql            # tables + enums + indexes
  02_rls.sql               # Row Level Security
  03_seed.sql              # 504 catalog stickers (42 teams x 11 players + a text Team Crest)
  04_triggers.sql          # auto-create profile on sign-up
  05_messaging.sql         # conversations + messages + RPC + realtime
  06_location.sql          # post latitude/longitude/country
  07_safety.sql            # blocks + reports (block-aware RLS)
  08_hardening.sql         # lock down SECURITY DEFINER functions
  09_hardening2.sql        # pre-launch API/UGC hardening (expiry, blocks, constraints, moderation)
  10_push.sql              # device_tokens + on-message trigger -> notify-message (APNs)
  stickers_seed.csv        # catalog as CSV (easy to replace)
  functions/
    delete-account/        # edge function: in-app account deletion
    notify-message/        # edge function: APNs push on new message

StickerMatch/              # iOS source (project generated by XcodeGen)
  StickerMatchApp.swift
  Info.plist, *.entitlements, PrivacyInfo.xcprivacy
  Assets.xcassets/         # AppIcon (placeholder — replace icon-1024.png)
  Config/                  # AppConfig + Secrets(.example).xcconfig
  Models/ Services/ ViewModels/
  Views/                   # Album, Marketplace, Matches, Messages, Profile, Safety, Components
project.yml                # XcodeGen spec
```

---

## 1. Set up Supabase

1. Create a project at <https://supabase.com>.
2. **SQL Editor** → run in order: `01` → `02` → `03` → `04` → `05` → `06` → `07` → `08` → `09` → `10`.
3. Verify the catalog: `select count(*) from public.stickers;` → **504**.
   (Replace it via Table Editor → `stickers` → Import CSV, shaped like `stickers_seed.csv`.)
4. **Deploy the edge functions**:
   ```bash
   supabase functions deploy delete-account
   supabase functions deploy notify-message --no-verify-jwt
   ```
   (`notify-message` is called by a DB trigger, not a user, so it authenticates with a shared
   secret instead of a JWT — see §3.)
5. **Auth providers** (Authentication → Providers):
   - **Google** → enable; paste an OAuth Web client ID/secret (Google Cloud Console, with
     `https://<project>.supabase.co/auth/v1/callback` as the redirect URI).
     URL Configuration → Redirect URLs → add `stickermatch://login-callback`.
   - **Apple** → enable for release (needs paid Apple Developer setup; see checklist).
   - **Email** → **disable** (Authentication → Sign In / Providers → Email). The app is
     **OAuth-only** (no email/password UI), so the password sign-up endpoint is turned off to
     shrink the attack surface. Leaked-password protection is then **N/A** (no passwords exist).
6. Copy your **Project URL host** + **anon public key** (Project Settings → API).

---

## 2. Set up the iOS app

```bash
brew install xcodegen
xcodegen generate        # creates StickerMatch.xcodeproj (gitignored)
open StickerMatch.xcodeproj
```
`project.yml` pre-wires the `supabase-swift` package, Sign in with Apple capability, the
`stickermatch` URL scheme, the location usage string, the app icon, and the Info.plist ↔
xcconfig keys.

**Secrets:** edit `StickerMatch/Config/Secrets.xcconfig` (gitignored; a placeholder exists):
```
SUPABASE_HOST     = abcdwxyz.supabase.co        # host only, no https://
SUPABASE_ANON_KEY = <your anon public key>
```
> ⚠️ The app `fatalError`s on launch until these are real (by design).

Pick a Simulator/device, set your signing **Team** under Signing & Capabilities, and Run.
On first Google sign-in the DB trigger creates your profile automatically.

---

## 3. Push notifications (APNs)

New-message pushes are wired end-to-end (`10_push.sql` + the `notify-message` Edge Function +
in-app registration in `PushManager`). To switch them on:

1. **APNs Auth Key** — Apple Developer portal → Certificates, IDs & Profiles → **Keys → +**,
   enable **Apple Push Notifications service (APNs)**, download the one-time `AuthKey_XXXX.p8`
   and note its **Key ID**. Enable **Push Notifications** on the `com.stickermatch.app` App ID.
2. **Edge Function secrets** (Dashboard → Edge Functions → `notify-message` → Secrets):

   | Secret | Value |
   |---|---|
   | `PUSH_FUNCTION_SECRET` | must equal the Vault `push_function_secret` |
   | `APNS_KEY_ID` | the `.p8` Key ID |
   | `APNS_TEAM_ID` | your Apple Team ID |
   | `APNS_BUNDLE_ID` | `com.stickermatch.app` |
   | `APNS_PRIVATE_KEY` | full contents of the `.p8` (PEM) |
   | `APNS_ENV` | `sandbox` for dev/device-debug builds; `production` for TestFlight/App Store |

3. The DB trigger reads the function URL + shared secret from **Vault** — set once:
   ```sql
   select vault.create_secret('https://<ref>.supabase.co/functions/v1/notify-message', 'push_function_url');
   select vault.create_secret('<same value as PUSH_FUNCTION_SECRET>', 'push_function_secret');
   ```
4. Build & run on a **device** (Xcode provisions the Push capability) and accept the prompt.
   `aps-environment` in `StickerMatch.entitlements` is `development`; change it to `production`
   (and set `APNS_ENV=production`) for release archives — the sandbox/production APNs hosts differ.

Until the secrets are set, `notify-message` no-ops gracefully (returns `skipped: unconfigured`);
chat still works, you just won't get a banner.

---

## How it works

- **Album** — browse by **team** with a progress hero and per-team progress rings; tap a
  team for its players. Real **flags**, global search, and a single **copies counter** per
  player (0 = missing, 1 = have, 2+ = repeated/spares). Status is derived from the count.
- **Marketplace** — posts near you, **sorted by distance** with a radius control
  (50/100/250 km / Country); the widest option scopes to your **country** (not global), and it
  falls back to your **profile country** when location is off. Filter
  out your own posts; manage them under **My posts** (edit / swipe-to-delete). Each card has a
  **Message** button and the safety disclaimer.
- **Matches** — your album vs. nearby posts: *they have X you need* / *you have Y they need*,
  ranked by score, limited to your radius.
- **Messages** — realtime 1:1 chat (Supabase Realtime) with **APNs push notifications** on new
  messages (tapping the banner opens that chat) and an unread badge on the tab. Connecting never
  reveals a phone number; nicknames are snapshotted so profiles stay private.
- **Safety** — **block** (hides posts/matches/messages both ways, enforced in RLS) and
  **report** (reason + note) from any post, match, or chat; manage blocks in Profile.
- **Profile** — nickname, **country** (searchable picker) + **city** (MapKit autocomplete),
  meeting point, blocked users, sign out, and **real account deletion** (edge function wipes
  all your data via cascade).

### Privacy / data model
Your raw album (`user_stickers`) and profile are owner-private (RLS). Only what you publish in
a post is visible to others, and only to non-blocked users near you. Coordinates come from the
city you pick when posting; your live location is used on-device for sorting. `PrivacyInfo.xcprivacy`
declares the collected data (email, name, coarse location, user content).

---

## Before the App Store (not done — mostly external)

- Paid **Apple Developer Program**, real code signing, and **device testing**.
- **Sign in with Apple** functional (required since Google login is offered — Guideline 4.8).
- **App icon** — replace the placeholder `Assets.xcassets/AppIcon.appiconset/icon-1024.png`.
- **Privacy Policy** URL + App Store privacy disclosures; UGC terms/EULA at sign-up.
- Publish/verify the **Google OAuth** consent screen (it starts in "testing" mode).
- Age rating (messaging + marketplace → 17+) and legal review of player-name usage.

---

## Android (later)
Same Supabase backend, schema, and RLS. Planned: Kotlin + Jetpack Compose, MVVM with
`ViewModel` + `StateFlow`, `supabase-kt`, and a Repository layer mirroring the iOS Services.
No Android code in this MVP.
